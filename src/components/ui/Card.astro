---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = true } = Astro.props;
---
<div class={`bg-white border border-gray-200 rounded-2xl p-6 ${hover ? 'hover:shadow-lg transition-shadow duration-200' : ''} ${className}`}>
  <slot />
</div>
