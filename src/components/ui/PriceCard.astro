---
interface Props {
  showAnnual?: boolean;
  highlighted?: boolean;
  class?: string;
}

const { showAnnual = false, highlighted = false, class: className = '' } = Astro.props;
---
<div class={`bg-white rounded-2xl border-2 ${highlighted ? 'border-electric-lime shadow-xl shadow-purple/10' : 'border-gray-200'} p-8 ${className}`}>
  <div class="mb-6">
    <p class="text-sm font-semibold uppercase tracking-widest text-gray-400 mb-2">Standard Plan</p>
    <div class="flex items-baseline gap-2 mb-1">
      <span class="text-5xl font-extrabold text-coral" style="font-family: var(--font-heading);">£999</span>
      <span class="text-gray-400">/month</span>
    </div>
    <p class="text-sm text-gray-500">per dedicated operator</p>
  </div>

  <div class="mb-6 p-4 bg-cream rounded-xl">
    <p class="text-sm text-gray-600 mb-1">Annual plan</p>
    <div class="flex items-center justify-between">
      <span class="font-bold text-dark">£9,990/year</span>
      <span class="text-xs bg-electric-lime text-dark px-2 py-1 rounded-full font-semibold">Save £1,998</span>
    </div>
    <p class="text-xs text-gray-500 mt-1">Two months free</p>
  </div>

  <ul class="space-y-3 mb-8">
    {[
      '1 dedicated operator',
      '40 hours per week',
      'Matched to your timezone',
      'All platforms supported',
      'AI-trained, modern tooling',
      'Weekly reports & audit trail',
      'SOP documentation',
      'Free AI chatbot for your store',
      'Named account contact',
      'Cancel anytime'
    ].map(item => (
      <li class="flex items-start gap-3 text-sm">
        <svg class="w-5 h-5 text-purple flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M16.704 4.153a.75.75 0 01.143 1.052l-8 10.5a.75.75 0 01-1.127.075l-4.5-4.5a.75.75 0 011.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 011.05-.143z" clip-rule="evenodd" />
        </svg>
        <span class="text-gray-700">{item}</span>
      </li>
    ))}
  </ul>

  <a href="/contact" class="block w-full text-center bg-electric-lime text-dark font-bold py-4 rounded-xl hover:opacity-90 transition-opacity text-lg">
    Get started &rarr;
  </a>

  <p class="text-center text-xs text-gray-400 mt-4">No lock-in &middot; Cancel anytime &middot; GBP billing</p>
</div>
