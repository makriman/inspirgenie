---
import { serviceNavItems, platformNavItems, mainNav } from '../../data/navigation';
const currentPath = Astro.url.pathname;
---
<header id="site-header" class="fixed top-0 left-0 right-0 z-50 transition-all duration-300" role="banner">
  <div class="container">
    <nav class="flex items-center justify-between h-16 lg:h-20" aria-label="Main navigation">

      <!-- Logo -->
      <a href="/" class="flex-shrink-0" aria-label="inspir - home">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 108 32" fill="currentColor" class="h-8 w-auto logo-svg" aria-hidden="true">
          <text x="0" y="25" font-family="'Plus Jakarta Sans', system-ui, sans-serif" font-weight="800" font-size="28" letter-spacing="-0.5">inspir</text>
        </svg>
      </a>

      <!-- Desktop Nav -->
      <div class="hidden lg:flex items-center gap-8">

        <!-- Services Dropdown -->
        <div class="relative group">
          <button class={`nav-link flex items-center gap-1 font-medium transition-colors duration-150 py-2${currentPath.startsWith('/services') ? ' nav-link--active' : ''}`} aria-haspopup="true" aria-expanded="false">
            Services
            <svg class="w-4 h-4 transition-transform duration-150 group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="dropdown-menu absolute top-full left-0 mt-1 w-72 bg-white rounded-xl shadow-xl border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-150 p-2">
            {serviceNavItems.map(item => (
              <a href={item.href} class="block px-4 py-3 rounded-lg hover:bg-cream transition-colors">
                <div class="font-semibold text-dark text-sm">{item.label}</div>
                {item.description && <div class="text-xs text-gray-500 mt-0.5">{item.description}</div>}
              </a>
            ))}
            <div class="border-t border-gray-100 mt-2 pt-2">
              <a href="/services" class="block px-4 py-2 text-sm font-semibold text-purple hover:bg-cream rounded-lg transition-colors">
                View all services →
              </a>
            </div>
          </div>
        </div>

        <!-- Platforms Dropdown -->
        <div class="relative group">
          <button class={`nav-link flex items-center gap-1 font-medium transition-colors duration-150 py-2${currentPath.startsWith('/platforms') ? ' nav-link--active' : ''}`} aria-haspopup="true" aria-expanded="false">
            Platforms
            <svg class="w-4 h-4 transition-transform duration-150 group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="dropdown-menu absolute top-full left-0 mt-1 w-64 bg-white rounded-xl shadow-xl border border-gray-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-150 p-2">
            {platformNavItems.map(item => (
              <a href={item.href} class="block px-4 py-3 rounded-lg hover:bg-cream transition-colors">
                <div class="font-semibold text-dark text-sm">{item.label}</div>
                {item.description && <div class="text-xs text-gray-500 mt-0.5">{item.description}</div>}
              </a>
            ))}
            <div class="border-t border-gray-100 mt-2 pt-2">
              <a href="/platforms" class="block px-4 py-2 text-sm font-semibold text-purple hover:bg-cream rounded-lg transition-colors">
                All platforms →
              </a>
            </div>
          </div>
        </div>

        {mainNav.map(item => (
          <a href={item.href} class={`nav-link font-medium transition-colors duration-150${currentPath === item.href ? ' nav-link--active' : ''}`}>{item.label}</a>
        ))}

        <a href="/contact" class="header-cta ml-2 font-bold px-5 py-2.5 rounded-lg text-sm transition-all duration-200">
          Get started →
        </a>
      </div>

      <!-- Mobile: CTA + Hamburger -->
      <div class="flex lg:hidden items-center gap-3">
        <a href="/contact" class="mobile-header-cta font-bold px-4 py-2 rounded-lg text-sm transition-all duration-200">
          Get started
        </a>
        <button id="mobile-menu-toggle" class="hamburger-btn p-2 rounded-lg transition-colors" aria-label="Open menu" aria-expanded="false" aria-controls="mobile-menu">
          <svg id="hamburger-icon" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </nav>
  </div>
</header>

<!-- Mobile Menu Overlay -->
<div id="mobile-menu" class="fixed inset-0 z-40 bg-dark hidden" aria-hidden="true">
  <div class="flex flex-col h-full overflow-y-auto">
    <div class="flex items-center justify-between p-6 border-b border-white/10">
      <a href="/" class="flex-shrink-0">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 108 32" fill="white" class="h-8 w-auto" aria-hidden="true">
          <text x="0" y="25" font-family="'Plus Jakarta Sans', system-ui, sans-serif" font-weight="800" font-size="28" letter-spacing="-0.5">inspir</text>
        </svg>
      </a>
      <button id="mobile-menu-close" class="p-2 text-white hover:text-purple transition-colors" aria-label="Close menu">
        <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <nav class="flex-1 p-6 space-y-2">
      <!-- Services Accordion -->
      <div class="mobile-accordion">
        <button class="mobile-accordion-trigger w-full flex items-center justify-between text-white text-xl font-bold py-3 hover:text-purple transition-colors" style="font-family: var(--font-heading);">
          Services
          <svg class="w-5 h-5 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>
        <div class="mobile-accordion-content hidden pl-4 pb-2 space-y-2">
          {serviceNavItems.map(item => (
            <a href={item.href} class="block text-gray-300 hover:text-white transition-colors py-2">{item.label}</a>
          ))}
          <a href="/services" class="block text-purple font-semibold py-2">All services →</a>
        </div>
      </div>

      <!-- Platforms Accordion -->
      <div class="mobile-accordion">
        <button class="mobile-accordion-trigger w-full flex items-center justify-between text-white text-xl font-bold py-3 hover:text-purple transition-colors" style="font-family: var(--font-heading);">
          Platforms
          <svg class="w-5 h-5 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>
        <div class="mobile-accordion-content hidden pl-4 pb-2 space-y-2">
          {platformNavItems.map(item => (
            <a href={item.href} class="block text-gray-300 hover:text-white transition-colors py-2">{item.label}</a>
          ))}
          <a href="/platforms" class="block text-purple font-semibold py-2">All platforms →</a>
        </div>
      </div>

      <a href="/pricing" class="block text-white text-xl font-bold py-3 hover:text-purple transition-colors" style="font-family: var(--font-heading);">Pricing</a>
      <a href="/about" class="block text-white text-xl font-bold py-3 hover:text-purple transition-colors" style="font-family: var(--font-heading);">About</a>
      <a href="/blog" class="block text-white text-xl font-bold py-3 hover:text-purple transition-colors" style="font-family: var(--font-heading);">Blog</a>
      <a href="/faq" class="block text-white text-xl font-bold py-3 hover:text-purple transition-colors" style="font-family: var(--font-heading);">FAQ</a>
    </nav>

    <div class="p-6 border-t border-white/10">
      <a href="/contact" class="block w-full text-center bg-purple text-white font-bold py-4 rounded-xl text-lg hover:bg-[#6d28d9] transition-colors">
        Get started →
      </a>
    </div>
  </div>
</div>

<style>
  /* ── Transparent state (over hero) ── */
  #site-header {
    background: transparent;
  }
  #site-header .logo-svg { color: white; }
  #site-header .nav-link { color: rgba(255,255,255,0.88); }
  #site-header .nav-link:hover { color: white; }
  #site-header .nav-link--active {
    color: white;
    text-decoration: underline;
    text-decoration-color: #B6F03C;
    text-underline-offset: 4px;
  }
  #site-header .header-cta {
    background: rgba(255,255,255,0.95);
    color: #0f0a1a;
  }
  #site-header .header-cta:hover {
    background: white;
  }
  #site-header .mobile-header-cta {
    background: rgba(255,255,255,0.95);
    color: #0f0a1a;
  }
  #site-header .hamburger-btn { color: white; }
  #site-header .hamburger-btn:hover { background: rgba(255,255,255,0.12); }

  /* ── Scrolled state (white bar) ── */
  #site-header.scrolled {
    background: white;
    border-bottom: 1px solid #e5e7eb;
    box-shadow: 0 1px 8px rgba(0,0,0,0.07);
  }
  #site-header.scrolled .logo-svg { color: #0f0a1a; }
  #site-header.scrolled .nav-link { color: #0f0a1a; }
  #site-header.scrolled .nav-link:hover { color: #7c3aed; }
  #site-header.scrolled .nav-link--active {
    color: #7c3aed;
    text-decoration: underline;
    text-decoration-color: #B6F03C;
    text-underline-offset: 4px;
  }
  #site-header.scrolled .header-cta {
    background: #7c3aed;
    color: white;
  }
  #site-header.scrolled .header-cta:hover {
    background: #6d28d9;
  }
  #site-header.scrolled .mobile-header-cta {
    background: #7c3aed;
    color: white;
  }
  #site-header.scrolled .hamburger-btn { color: #0f0a1a; }
  #site-header.scrolled .hamburger-btn:hover { background: #f5f5f4; }
</style>

<script>
  const header = document.getElementById('site-header');
  const mobileToggle = document.getElementById('mobile-menu-toggle');
  const mobileClose = document.getElementById('mobile-menu-close');
  const mobileMenu = document.getElementById('mobile-menu');

  // Scroll behaviour
  function updateHeader() {
    if (window.scrollY > 60) {
      header?.classList.add('scrolled');
    } else {
      header?.classList.remove('scrolled');
    }
  }
  window.addEventListener('scroll', updateHeader, { passive: true });
  updateHeader();

  // Mobile menu
  function openMenu() {
    mobileMenu?.classList.remove('hidden');
    mobileMenu?.setAttribute('aria-hidden', 'false');
    mobileToggle?.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }

  function closeMenu() {
    mobileMenu?.classList.add('hidden');
    mobileMenu?.setAttribute('aria-hidden', 'true');
    mobileToggle?.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  mobileToggle?.addEventListener('click', openMenu);
  mobileClose?.addEventListener('click', closeMenu);

  // Mobile accordions
  document.querySelectorAll('.mobile-accordion-trigger').forEach(trigger => {
    trigger.addEventListener('click', () => {
      const content = trigger.nextElementSibling as HTMLElement;
      const icon = trigger.querySelector('svg') as SVGElement;
      const isOpen = !content.classList.contains('hidden');
      content.classList.toggle('hidden', isOpen);
      if (icon) icon.style.transform = isOpen ? 'rotate(0deg)' : 'rotate(180deg)';
    });
  });

  // Close menu on link click
  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', closeMenu);
  });

  // Escape key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeMenu();
  });
</script>
